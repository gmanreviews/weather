@{
    ViewBag.Title = "Home Page";
}
<div class="jumbotron">
    <h1>Test Weather App</h1>
    <p class="lead">Filling some gaps here so it doesn't look that bad</p>
    <p>It looks pretty bad doesn't it???</p>
</div>

<div class="row">
    <div class="col-md-6 current">
        @{Html.RenderAction("Current", "Weather", new { id = 1 });}
    </div>
    <div class="col-md-6 current">
        @{Html.RenderAction("Current", "Weather", new { id = 2 });}
    </div>
    <div class="col-md-6 current">
        @{Html.RenderAction("Current", "Weather", new { id = 3 });}
    </div>
    <div class="col-md-6 current">
        @{Html.RenderAction("Current", "Weather", new { id = 4 });}
    </div>
</div>